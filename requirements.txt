numpy>=1.26
scipy>=1.11
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3
# хранилище демо-сборки — sqlite3 (входит в стандартную библиотеку Python)
# веб-визуализация — Leaflet 1.9 (подключается по CDN в web/index.html)
# продакшен-хранилище — PostgreSQL 16 + PostGIS 3.4 + TimescaleDB (db/schema_postgres.sql)
